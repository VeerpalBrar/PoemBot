- if user_signed_in?
    =link_to('Sign out', destroy_user_session_path, method: :delete)
- else
    =link_to('Sign In', new_user_session_path)
= react_component('PoemsList', props: {poems: @poems, loggedIn: user_signed_in? }, prerender: false)
